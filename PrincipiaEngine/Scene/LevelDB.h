
//Creates a global database of the level folders
//probably shouldn't do this but whatever lets yolocode

#include <iostream>
#include <vector>
#include <string>

//static std::vector<std::string> g_LevelNames;
//static std::string g_LevelsDirectory = "../Assets/Levels/";

// inline xxh::hash32_t xxhasher(std::string s) { return xxh::xxhash<32, char>(s.c_str(), 0); }

//static void CreateDatabase() {
//	g_LevelNames.reserve(2);
//	g_LevelNames.emplace_back(std::string("Test"));
//	g_LevelNames.emplace_back(std::string("Level1"));
//}

///static uint32_t s_AllocCount = 0;
//void* operator new(size_t size) {
//	s_AllocCount++;
//	std::cout << "Allocating: " << size << "bytes \n";
//	return malloc(size);
//} 
